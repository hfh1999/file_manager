find_package(Catch2 CONFIG REQUIRED)
MESSAGE(STATUS "Catch2: " ${CATCH_INCLUDE_DIRS})

add_executable(testLog testLog.cpp)

# target_link_libraries(testLog PRIVATE Catch2::Catch2WithMain)

add_test(NAME test1 COMMAND testLog)